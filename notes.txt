Model 5 is just the first common lines between all MDATAs.
MDATA2=function(n,p,d=2,c) {
    
    data=array(0,dim=c(n,p,d),dimnames=list(c(1:n),c(1:p),c("var1","var2")))
    
    x=(1:p/p)
    
    model <- RMbiwm(nudiag=c(1.2, 0.6), nured=1, rhored=0.6, cdiag=c(1, 1),s=c(0.02, 0.016, 0.01))

for (i in 1:n) {
    
    a=matrix(RFsimulate(model, x),p,2)
    
    data[i,,]=cbind(a[,1],a[,2])
}

MDATA12 contains the code for Model 6 of the paper Dai2019.
MDATA11 contains the code for Model 7 of the paper Dai2019.
MDATA3 contains the code for Model 8 of the paper Dai2019.
MDATA1 contains the code for Model 9 of the paper Dai2019.
MDATA4 contains the code for Model 10 of the paper Dai2019.

These functions depend on 4 parameters:
    p is the number of data points.
    n is the number of curves of each var.
    c is the contamination level.
    d is the number of variables.

Los outliers suelen ser por niveles elevados y oscilaciones acusadas.

Si quiero hacer la validación con los modelos de Dai2019 tengo que ver
como implementar Random Fields en Python. Igual puedo hacer un buen prompt
donde se explica lo que hacer el paper (copiar su texto) y pasarselo a
ChatGPT.